{
  "name": "my-homepage",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "publish:cf": "pnpm build && find dist -type f -size +24M -print -delete && npx wrangler pages deploy dist --project-name=my-homepage --branch=main --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^6.1.6"
  }
}
